# Gem installation source site
source 'http://rubygems.org'

# Gem for PostgreSql database adapter
gem 'pg'
# Gem for excel generation
gem 'axlsx'
#gem for calling API
gem 'rest-client', '~> 1.8'
#gem for parsing api json response
gem 'json'
#gem for ruby connect with GUI
gem 'green_shoes', '~> 1.1', '>= 1.1.374'
